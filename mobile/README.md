# ACTIFY Mobile

Flutter client for ACTIFY.

## Local setup

From this directory:

```bash
flutter create .
flutter pub get
flutter run
```

The `flutter create .` command generates the Android/iOS platform folders that are intentionally not checked in as hand-written scaffold files.
