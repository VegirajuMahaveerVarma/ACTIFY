class SupabaseClient:
    """Database adapter placeholder for Supabase integration."""

    def __init__(self, url: str = '', key: str = '') -> None:
        self.url = url
        self.key = key


client = SupabaseClient()
